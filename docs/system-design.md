# System Design

Frontend Dashboard → User interaction

Backend API → Handles SOS requests

AI Model → Predicts disaster severity

Storage Layer → Stores SOS data

## Flow

User → Frontend → Backend → AI → Response → Dashboard